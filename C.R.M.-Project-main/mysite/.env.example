# Django Database Configuration for MS SQL Server
# Copy this file to .env# =============================================================================
# DJANGO SETTINGS
# =============================================================================

# SECURITY WARNING: Generate a new secret key for production
# You can generate one using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-ss_8^n9y&2l67iwpc&tfxcd1qn-y%yiiv2dejrgp8ym^rx-&hf

# Set to False in production
DEBUG=True

# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# =============================================================================
# DATABASE SETTINGS - MS SQL SERVER
# =============================================================================

# Database name
DB_NAME=crm_database

# Database server address (use your server name or IP)
# Examples:
#   localhost          - For local server
#   .\SQLEXPRESS       - For SQL Express named instance
#   LAPTOP-F4S2FA88    - For specific machine
#   192.168.1.100      - For remote server
DB_HOST=localhost

# Database port (usually 1433)
DB_PORT=1433

# ODBC Driver (check installed drivers: python -c "import pyodbc; print(pyodbc.drivers())")
DB_DRIVER=ODBC Driver 17 for SQL Server

# =============================================================================
# AUTHENTICATION METHOD
# =============================================================================

# For Windows Authentication (Recommended for Development):
# Leave DB_USER and DB_PASSWORD empty
DB_USER=
DB_PASSWORD=
DEBUG=True

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1
